---
title: "Ergebnisbericht als R Notebook"
output: html_notebook
---

Für Kolloquims-Vorträge oder den Austausch mit den BetreuerInnen eignet sich ein Ergebnisbericht besser als das gesamte Buch. Um beides separat zu halten, kann man den Ergebnisbericht in einer eigenen .Rmd-Datei verfassen. Beginnt der Dateiname mit einem Unterstrich `_` wird die Datei auch nicht für das Buch berücksichtigt.

Dieser Ergebnisbericht ist ein [R Markdown](http://rmarkdown.rstudio.com) Notebook, das beim Abspeichern automatisch eine self-contained html-Seite `_Ergebnisbericht.nb.html` im Projektordner erstellt, die einfach per Mail verschickt oder auf einen Server hochgeladen werden kann.


----

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 

```{r}
plot(cars)
```

Add a new chunk by clicking the *Insert Chunk* button on the toolbar or by pressing *Ctrl+Alt+I*.

When you save the notebook, an HTML file containing the code and output will be saved alongside it (click the *Preview* button or press *Ctrl+Shift+K* to preview the HTML file).




